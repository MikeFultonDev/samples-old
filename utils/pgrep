str=$1
ds=`opercmd 'd parmlib' | awk '/ENTRY  FLAGS  VOLUME  DATA SET/ { afterHeader=1; next; } /[ ]+[0-9]+/ { if (afterHeader) { print $4; next } }'`
for d in $ds ; do
	dgrep ${str} ${d} 2>/dev/null
done
exit 1
